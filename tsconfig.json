{
  // Type-checking only — `noEmit` means nothing is compiled and there is still
  // no build step. Types come from JSDoc in plain .js files, so what runs is
  // what is on disk.
  //
  //   npm run typecheck
  //
  // Server code and browser code are checked separately (see
  // tsconfig.browser.json) because they have different globals: `process` and
  // `Buffer` on one side, `window` and `document` on the other.
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2023"],
    "types": ["node"],

    "allowJs": true,
    "checkJs": true,
    "noEmit": true,

    // Pragmatic to begin with: catch wrong shapes, typos and bad arguments
    // without demanding annotations everywhere. Tighten as coverage grows.
    "strict": false,
    "noImplicitAny": false,
    "strictNullChecks": false,

    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": [
    "shared/src/**/*.js",
    "habiterall-personal/src/**/*.js",
    "habiterall-cloud/src/**/*.js",

    // The website generator. Node code, so it belongs on this side — its
    // browser half is `site/static/site.js` and is in tsconfig.browser.json.
    // Listed by extension rather than as `site/**/*` so that adding a script
    // under `site/static/` cannot silently land in the config with the wrong
    // globals and be checked against a DOM that is not there.
    "site/*.js",
    "site/*.mjs",
    "site/templates/*.mjs"
  ],
  "exclude": ["node_modules", "**/node_modules"]
}
